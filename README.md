# `@lucid-softworks/string-common-prefix`

Find the longest common prefix in a list of strings.

```ts
import { commonPrefix } from "@lucid-softworks/string-common-prefix";

commonPrefix(["interact", "internet"]);
```

See the exported types and tests for exact edge-case behavior.
